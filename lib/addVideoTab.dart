import 'package:flutter/material.dart';
import 'constants.dart';

class AddVideoTab extends StatelessWidget {
  const AddVideoTab({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: kContainerThemeDecoration,
    );
  }
}
