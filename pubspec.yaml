name: mentor_digishala
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  font_awesome_flutter:
  firebase_auth:
  firebase_core:
  cloud_firestore:
  google_sign_in: ^4.5.1
  shared_preferences: ^0.5.8
  firebase_database:
  fluttertoast: ^7.0.2
  file_picker: ^1.13.3
  firebase_storage:
  url_launcher: ^5.5.0
  rflutter_alert: ^1.0.3
  google_nav_bar:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
    #   - images/a_dot_ham.jpeg
    #fonts:
    #- family: Schyler
